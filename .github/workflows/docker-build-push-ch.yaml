name: Docker Image Build on tag or release for CH version

on:
  push:
    tags:
      - "*"
  release:
    types: [published]

env:
  ORG: stackmon
  PROJECT: status-dashboard-v3-front

jobs:
  push_if_tag:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            "${{ secrets.REGISTRY }}/${{ env.ORG }}/${{ env.PROJECT }}"
          tags: |
            type=semver,pattern={{version}}
          flavor: |
            latest=false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push ch-version
        uses: docker/build-push-action@v7
        with:
          context: .
          tags: ${{ steps.meta.outputs.tags }}-ch
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          secrets: |
            SD_BACKEND_URL=${{ vars.SD_BACKEND_URL_CH_PROD }}
            SD_CLIENT_ID=${{ vars.SD_CLIENT_ID }}
            SD_AUTHORITY_URL=${{ vars.SD_AUTHORITY_URL }}
            SD_ANALYTICS_URL=${{ vars.SD_ANALYTICS_URL }}
            SD_ANALYTICS_ID=${{ vars.SD_ANALYTICS_ID_PROD_CH }}
            SD_NAME=${{ vars.SD_NAME_CH }}
            SD_GIT_SHA=${{ github.sha }}
